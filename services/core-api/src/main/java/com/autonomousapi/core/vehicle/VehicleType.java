package com.autonomousapi.core.vehicle;

/** Tipo do veículo (spec 08, item 4) — usado pra escolher ícone em listagem. Opcional. */
public enum VehicleType {
    CARRO,
    MOTO,
    VAN,
    CAMINHAO,
    ONIBUS
}
